import React from 'react';
import styled from 'styled-components';

const Nav = (): JSX.Element => {
  return <Temp> Nav </Temp>;
};

const Temp = styled.div`
  position: fixed;
  top: 0px;
  left: 0px;
  width: 100vw;
  height: 80px;
  background-color: black;
  z-index: 100;
`;

export default Nav;
